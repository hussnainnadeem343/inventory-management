@extends('layouts.app') @section('title','Stock History') @section('content')<div class="d-flex justify-content-between align-items-center mb-3"><div><h2 class="h5 mb-0">{{$product->item_name}}</h2><small class="text-secondary">{{$product->sku}}</small></div><a class="btn btn-light" href="{{route('stock.index')}}">Back to Stock</a></div><div class="card"><div class="table-responsive"><table class="table table-hover align-middle mb-0"><thead class="table-light"><tr><th>Date</th><th>Type</th><th>Quantity</th><th>User</th></tr></thead><tbody>@forelse($transactions as $transaction)<tr><td>{{$transaction->created_at->format('d-M-Y h:i A')}}</td><td><span class="badge text-bg-{{$transaction->transaction_type==='SALE'?'warning':'success'}}">{{str($transaction->transaction_type)->replace('_',' ')}}</span></td><td class="fw-semibold text-{{$transaction->transaction_type==='SALE'?'danger':'success'}}">{{$transaction->transaction_type==='SALE'?'-':'+'}}{{number_format($transaction->quantity,2)}}</td><td>{{$transaction->creator->name}}</td></tr>@empty<tr><td colspan="4" class="text-center py-4">No stock history found.</td></tr>@endforelse</tbody></table></div></div><div class="mt-3">{{$transactions->onEachSide(1)->links()}}</div>@endsection
