@extends('layouts.app') @section('title','Stock Management') @section('content')
<div class="card p-3 mb-3"><form method="get" class="row g-2 align-items-end"><div class="col-lg-4"><label class="form-label">Search</label><input class="form-control" name="search" value="{{$filters['search']}}" placeholder="Product name or SKU"></div><div class="col-sm-6 col-lg-2"><label class="form-label">Brand</label><select class="form-select" name="brand_id"><option value="">All Brands</option>@foreach($brands as $brand)<option value="{{$brand->id}}" @selected($filters['brand_id']==$brand->id)>{{$brand->name}}</option>@endforeach</select></div><div class="col-sm-6 col-lg-2"><label class="form-label">Category</label><select class="form-select" name="category_id"><option value="">All Categories</option>@foreach($categories as $category)<option value="{{$category->id}}" @selected($filters['category_id']==$category->id)>{{$category->name}}</option>@endforeach</select></div><div class="col-sm-6 col-lg-2"><label class="form-label">Per page</label><select class="form-select" name="per_page">@foreach([10,20,30,50,100] as $size)<option @selected($perPage===$size)>{{$size}}</option>@endforeach</select></div><div class="col-lg-2 d-flex gap-2"><button class="btn btn-primary">Search</button><a class="btn btn-outline-secondary" href="{{route('stock.index')}}">Reset</a></div></form></div>
<div class="text-end mb-3"><a class="btn btn-outline-success" href="{{route('stock.export',request()->except('page'))}}">Export Excel</a></div><div class="card"><div class="table-responsive"><table class="table table-hover align-middle mb-0"><thead class="table-light"><tr><th>Product / SKU</th><th>Brand</th><th>Category</th><th>Stock In</th><th>Sold</th><th>Remaining</th><th>Pack Size</th><th>Status</th><th>Actions</th></tr></thead><tbody>@forelse($products as $product)<tr><td><strong>{{$product->item_name}}</strong><br><small>{{$product->sku?:'-'}}</small></td><td>{{$product->brand->name}}</td><td>{{$product->category->name}}</td><td>{{number_format($product->quantity,2)}}</td><td>{{number_format($product->sold_quantity,2)}}</td><td>@if($product->remaining_quantity<=0)<span class="badge text-bg-danger">Out of Stock</span>@else<span class="badge text-bg-success">{{number_format($product->remaining_quantity,2)}}</span>@endif</td><td>{{$product->pack_label?:'-'}}</td><td>{{ucfirst($product->status)}}</td><td><div class="d-flex flex-wrap gap-1"><a class="btn btn-sm btn-outline-success" href="{{route('stock.add-form',$product)}}">Add Stock</a><a class="btn btn-sm btn-success {{$product->remaining_quantity<=0?'disabled':''}}" href="{{$product->remaining_quantity<=0?'#':route('stock.sell-form',$product)}}">Sell</a><a class="btn btn-sm btn-outline-secondary" href="{{route('stock.history',$product)}}">History</a></div></td></tr>@empty<tr><td colspan="9" class="text-center py-4">No products found.</td></tr>@endforelse</tbody></table></div></div><div class="mt-3">{{$products->onEachSide(1)->links()}}</div>@endsection
