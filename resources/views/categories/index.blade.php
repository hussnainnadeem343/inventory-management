@extends('layouts.app') @section('title','Categories') @section('content')
<div class="d-flex justify-content-between mb-3"><form method="get"><select class="form-select" name="per_page" onchange="this.form.submit()">@foreach([10,20,30,50,100] as $size)<option @selected($perPage===$size)>{{$size}}</option>@endforeach</select></form><a class="btn btn-primary" href="{{route('categories.create')}}">+ Add Category</a></div>
<div class="card"><div class="table-responsive"><table class="table table-hover align-middle mb-0"><thead class="table-light"><tr><th>ID</th><th>Category Name</th><th>Description</th><th>Status</th><th>Created By</th><th>Created At</th><th>Actions</th></tr></thead><tbody>@forelse($categories as $category)<tr><td>{{$category->id}}</td><td>{{$category->name}}</td><td>{{$category->description?:'-'}}</td><td>{{ucfirst($category->status)}}</td><td>{{$category->creator->name}}</td><td>{{$category->created_at->format('d-M-Y h:i A')}}</td><td><div class="d-flex gap-1"><a class="btn btn-sm btn-outline-primary" href="{{route('categories.edit',$category)}}">Edit</a>@if(auth()->user()->isSuperAdmin())<form method="post" action="{{route('categories.destroy',$category)}}" data-confirm="Are you sure you want to delete this category?">@csrf @method('delete')<button class="btn btn-sm btn-outline-danger">Delete</button></form>@endif</div></td></tr>@empty<tr><td colspan="7" class="text-center py-4">No categories found.</td></tr>@endforelse</tbody></table></div></div><div class="mt-3">{{$categories->onEachSide(1)->links()}}</div>@endsection
