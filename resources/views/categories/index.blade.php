@extends('layouts.app') @section('title','Categories') @section('content')@if(auth()->user()->isSuperAdmin())<div class="text-end mb-3"><a class="btn btn-primary" href="{{route('categories.create')}}">+ Add Category</a></div>@endif<div class="card"><div class="table-responsive"><table class="table table-hover align-middle mb-0"><thead class="table-light"><tr><th>ID</th><th>Category Name</th><th>Description</th><th>Status</th><th>Created By</th><th>Created At</th>@if(auth()->user()->isSuperAdmin())<th>Actions</th>@endif</tr></thead><tbody>@forelse($categories as $category)<tr><td>{{$category->id}}</td><td>{{$category->name}}</td><td>{{$category->description?:'-'}}</td><td>{{ucfirst($category->status)}}</td><td>{{$category->creator->name}}</td><td>{{$category->created_at->format('d-M-Y h:i A')}}</td>@if(auth()->user()->isSuperAdmin())<td><div class="d-flex gap-1"><a class="btn btn-sm btn-outline-primary" href="{{route('categories.edit',$category)}}">Edit</a><form method="post" action="{{route('categories.destroy',$category)}}" data-confirm="Are you sure you want to delete this category?">@csrf @method('delete')<button class="btn btn-sm btn-outline-danger">Delete</button></form></div></td>@endif</tr>@empty<tr><td colspan="7" class="text-center py-4">No categories found.</td></tr>@endforelse</tbody></table></div></div><div class="mt-3">{{$categories->links()}}</div>@endsection
